{
  "scripts": {
    "start": "elm reactor",
    "watch": "echo *DISABLED* parcel serve src/index.html --port 8088 --no-cache --out-file index.html",
    "build": "echo *DISABLED* parcel build src/index.html --no-source-maps --no-cache --out-file index.html"
  },
  "dependencies": {
    "elm": "^0.19.0"
  },
  "devDependencies": {
    "parcel": "^1.9.7",
    "parcel-bundler": "^1.9.7",
    "parcel-plugin-elm": "^0.1.3-alpha"
  }
}
